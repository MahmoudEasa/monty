#include "monty.h"

/**
 * handle_nop - do nothing
 * @head: pointer to first node
 * @line_num: number of lines
*/

void handle_nop(stack_t **head, unsigned int line_num)
{
	(void)head;
	(void)line_num;
}
